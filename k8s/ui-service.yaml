apiVersion: v1
kind: Service
metadata:
  name: agentgateway-ui
  namespace: agentgateway-system
  labels:
    app: agentgateway
    component: ui
spec:
  type: ClusterIP
  ports:
  - name: admin
    port: 15000
    targetPort: 15000
    protocol: TCP
  selector:
    # These selectors match the kgateway-managed proxy pods
    # kgateway uses this label for gateway-provisioned pods
    gateway.networking.k8s.io/gateway-name: agentgateway
